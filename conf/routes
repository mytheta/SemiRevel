# Routes Config
#
# This file defines all application routes (Higher priority routes first)
#

module:testrunner
# module:jobs


POST    /SemiRevel/login                                     Authentication.Login
GET     /SemiRevel/logout                                    Authentication.Logout
GET     /SemiRevel/                                          App.Index
GET     /SemiRevel/home                                      MaterialApi.Home
POST    /SemiRevel/material/:grade/:user_id                  MaterialApi.PostMaterial
GET     /SemiRevel/regist                                    MaterialApi.IndexMaterial
GET     /SemiRevel/materials/grade/                          MaterialApi.SelectGrade
GET     /SemiRevel/my_materials/                             MaterialApi.MyMaterials
GET     /SemiRevel/materials/grade/:grade                    MaterialApi.GradeMaterials
GET     /SemiRevel/materials/:grade/:user_id/:file_path      MaterialApi.File
GET     /SemiRevel/password/                                 Password.Index
GET     /SemiRevel/password/input                            Password.Input
POST    /SemiRevel/password/change                           Password.Password
GET     /SemiRevel/material/delete                           MaterialApi.DeleteMaterial


# Ignore favicon requests
GET     /favicon.ico                            404

# Map static resources from the /app/public folder to the /public path
GET     /public/*filepath                       Static.Serve("public")

# Catch all, this will route any request into the controller path
#
#                    **** WARNING ****
# Enabling this exposes any controller and function to the web.
# ** This is a serious security issue if used online **
#
# For rapid development uncomment the following to add new controller.action endpoints
# without having to add them to the routes table.
# *       /:controller/:action                    :controller.:action
